{
  "name": "river-agent",
  "version": "0.1.0",
  "private": true,
  "description": "River Agent — WhatsApp sales AI (360dialog MCP OAuth + CF Worker doorbell + Cursor Automation)",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "tsc --noEmit",
    "test:webhook": "node scripts/simulate-webhook.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260828.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.33.0"
  },
  "engines": {
    "node": ">=20"
  }
}
