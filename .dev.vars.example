# Optional Messaging API key (channel) — required for Path Direct go-live
D360_API_KEY=
D360_PHONE_NUMBER_ID=

# Bearer for River MCP /mcp and POST /send
SEND_AUTH_TOKEN=dev-send-token

# Optional — only if REPLY_MODE=automation
CURSOR_WEBHOOK_URL=
CURSOR_API_KEY=
